global:
  scrape_interval: 250ms       # 4Hz sampling for microsecond drift capture
  evaluation_interval: 250ms

scrape_configs:
  - job_name: 'vinntry-native-daemon'
    static_configs:
      - targets: ['host.docker.internal:9101'] # Connects to native C++ binary port